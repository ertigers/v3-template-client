# 光伏电站布局设计器 - Cursor AI 编程助手规则

## 🎯 项目概述
这是一个基于Vue3的光伏电站PV面板布局设计器，支持设备拖拽、画布操作、旋转缩放等交互功能。

## 🛠 技术栈
- **框架**: Vue 3 + Composition API + `<script setup>`
- **UI组件**: Element Plus
- **图形引擎**: AntV X6 (v2.18.1)
- **构建工具**: Vite 5.x
- **状态管理**: Pinia + Pinia Persist
- **路由**: Vue Router 4.x
- **语言**: JavaScript ES6+ (支持TypeScript提示)
- **样式**: SCSS + PostCSS
- **国际化**: Vue I18n
- **工具库**: @vueuse/core, dayjs, lodash

## 📋 编码规范与最佳实践

### Vue 3 组合式API规范
```javascript
// ✅ 推荐写法
<script setup>
import { ref, reactive, computed, onMounted } from 'vue'

// 响应式数据
const loading = ref(false)
const formData = reactive({
  name: '',
  type: ''
})

// 计算属性
const isValid = computed(() => formData.name && formData.type)

// 方法
const handleSubmit = async () => {
  loading.value = true
  try {
    await submitForm(formData)
  } finally {
    loading.value = false
  }
}

// 生命周期
onMounted(() => {
  initData()
})
</script>
```

### 命名约定
- **组件**: PascalCase (DevicePanel.vue, EditLayout.vue)
- **变量/函数**: camelCase (deviceList, handleClick)
- **常量**: UPPER_SNAKE_CASE (PANEL_WIDTH, DEFAULT_CONFIG)
- **文件夹**: kebab-case (components, utils)
- **CSS类**: kebab-case (.device-panel, .edit-layout)
- **接口/类型**: PascalCase (UserInfo, ApiResponse)

### 代码组织结构
```javascript
<script setup>
// 1. 导入
import { ref } from 'vue'
import { ElMessage } from 'element-plus'

// 2. 定义props和emits
const props = defineProps({
  data: Object
})
const emit = defineEmits(['update', 'delete'])

// 3. 响应式数据
const state = reactive({})
const loading = ref(false)

// 4. 计算属性
const computedValue = computed(() => {})

// 5. 监听器
watch(() => props.data, (newVal) => {})

// 6. 方法定义
const methods = {
  init() {},
  save() {},
  delete() {}
}

// 7. 生命周期钩子
onMounted(() => {})
onUnmounted(() => {})

// 8. 模板引用
const formRef = ref()

// 9. 暴露给模板的数据和方法
defineExpose({
  validate: () => formRef.value?.validate()
})
</script>
```

## 🎨 X6 图形引擎特定规则

### 坐标系统处理
```javascript
// ✅ 正确的坐标转换
const handleDrop = (e) => {
  const point = graph.clientToLocal(e.clientX, e.clientY)
  // 使用转换后的本地坐标
}

// ✅ 旋转中心计算
const rotateNode = (node, angle) => {
  const bbox = node.getBBox()
  const center = bbox.getCenter()
  node.rotate(angle, center)
}
```

### 事件管理
```javascript
// ✅ 事件监听器管理
onMounted(() => {
  graph.on('node:click', handleNodeClick)
  graph.on('node:dblclick', handleNodeDblClick)
  graph.on('blank:click', handleBlankClick)
})

onUnmounted(() => {
  // 清理事件监听器
  graph.off('node:click', handleNodeClick)
  graph.dispose()
})
```

### 性能优化策略
- 批量操作使用 `graph.batchUpdate()`
- 大量节点考虑虚拟渲染
- 避免频繁的DOM查询
- 使用防抖处理高频事件

## 🔧 项目特定业务规则

### PV面板管理
```javascript
// 设备状态管理
const deviceState = {
  isInCanvas: boolean,    // 是否在画布中
  isSelected: boolean,    // 是否选中
  rotation: number,       // 旋转角度
  position: { x, y },     // 位置坐标
  tiltAngle: number       // 倾角
}

// 选中状态样式
const SELECTED_STYLE = {
  stroke: '#ff6b6b',
  strokeWidth: 2
}
```

### 指南针旋转逻辑
```javascript
const calculateAngle = (centerX, centerY, mouseX, mouseY) => {
  const angle = Math.atan2(mouseY - centerY, mouseX - centerX)
  return ((angle * 180) / Math.PI + 360) % 360
}
```

### 状态栏信息显示
实时显示选中PV面板的关键信息：位置、角度、倾角等

## 🚨 错误处理与调试

### 防御性编程
```javascript
// ✅ 检查图形实例
if (!graph?.current) {
  console.warn('Graph instance not found')
  return
}

// ✅ 边界值验证
const normalizeAngle = (angle) => {
  return ((angle % 360) + 360) % 360
}

// ✅ 用户友好提示
try {
  await operation()
  ElMessage.success('操作成功')
} catch (error) {
  console.error('Operation failed:', error)
  ElMessage.error('操作失败，请重试')
}
```

### 调试辅助
```javascript
// 开发环境下的调试日志
const DEBUG = import.meta.env.DEV

const logOperation = (operation, data) => {
  if (DEBUG) {
    console.log(`[${operation}]`, data)
  }
}
```

## 📁 文件结构规范
```
src/
├── api/                    # API接口
├── assets/                 # 静态资源
├── components/             # 公共组件
│   ├── common/            # 通用组件
│   └── business/          # 业务组件
├── composables/           # 组合式函数
├── config/                # 配置文件
├── directives/            # 自定义指令
├── hooks/                 # Vue hooks
├── layouts/               # 布局组件
├── plugins/               # 插件
├── routers/               # 路由配置
├── stores/                # Pinia状态管理
├── styles/                # 全局样式
├── utils/                 # 工具函数
└── views/                 # 页面组件
    └── station/
        └── create/
            └── components/
                └── EditLayout.vue
```

## ⚡ 性能优化指南

### Vue 3 性能最佳实践
- 使用 `shallowRef` 处理大数据结构
- `computed` 替代复杂的 template 表达式
- 合理使用 `v-memo` 优化列表渲染
- 组件懒加载：`defineAsyncComponent`

### 内存管理
```javascript
// ✅ 清理副作用
onUnmounted(() => {
  // 清理定时器
  clearInterval(timer)
  // 清理事件监听
  window.removeEventListener('resize', handleResize)
  // 清理图形实例
  graph?.dispose()
})
```

## 🚫 禁止事项
- ❌ 不要混用 Options API 和 Composition API
- ❌ 不要在 template 中写复杂业务逻辑
- ❌ 不要直接操作 DOM，优先使用 Vue 响应式
- ❌ 不要忘记清理副作用（事件、定时器、观察者）
- ❌ 不要使用 CSS transform 旋转画布
- ❌ 不要在循环中创建响应式数据
- ❌ 不要过度使用 reactive，简单数据用 ref

## ✅ 推荐模式

### 组合式函数
```javascript
// composables/useCanvas.js
export function useCanvas() {
  const graph = ref(null)
  const selectedNodes = ref([])
  
  const initGraph = (container) => {
    graph.value = new Graph({ container })
  }
  
  const selectNode = (node) => {
    selectedNodes.value = [node]
  }
  
  return {
    graph: readonly(graph),
    selectedNodes: readonly(selectedNodes),
    initGraph,
    selectNode
  }
}
```

### 状态管理
```javascript
// stores/canvas.js
export const useCanvasStore = defineStore('canvas', () => {
  const devices = ref([])
  const selectedDevice = ref(null)
  
  const addDevice = (device) => {
    devices.value.push(device)
  }
  
  return {
    devices,
    selectedDevice,
    addDevice
  }
}, {
  persist: true // 持久化存储
})
```

### 错误边界
```javascript
// 全局错误处理
app.config.errorHandler = (err, vm, info) => {
  console.error('Vue Error:', err, info)
  ElMessage.error('系统错误，请联系管理员')
}
```

## 🔍 代码审查检查点
1. **组件职责单一**：每个组件只负责一个功能
2. **Props 验证**：定义明确的 props 类型和默认值
3. **事件命名**：使用动词形式，如 update、delete、change
4. **异步处理**：正确处理 Promise 和错误
5. **内存泄漏**：检查事件监听器和定时器清理
6. **可访问性**：添加适当的 aria 标签和键盘支持

## 🌐 国际化支持
```javascript
// 组件中使用国际化
const { t } = useI18n()

const messages = {
  'zh-CN': {
    save: '保存',
    cancel: '取消'
  },
  'en-US': {
    save: 'Save',
    cancel: 'Cancel'
  }
}
```

## 🧪 测试策略
- 单元测试：关键业务逻辑函数
- 组件测试：用户交互和数据流
- E2E测试：核心业务流程
- 性能测试：大数据量下的响应速度

## 🔐 安全考虑
- XSS防护：使用 `v-text` 而非 `v-html`
- CSRF保护：API请求包含 token
- 输入验证：前后端双重验证
- 权限控制：路由和组件级别的权限检查

---

**遵循这些规则可以确保代码质量、性能和可维护性。在开发过程中，优先考虑用户体验和代码的可读性。** 